// 시작 (23시 30분) => 끝 ()
const inputs = require("fs").readFileSync("./js/dev/stdin").toString().trim().split("\n");

console.log('inputs' , inputs);

const solution = (inputs) => {};

console.log(solution(inputs));
